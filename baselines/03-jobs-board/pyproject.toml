[project]
name = "jobs-board"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1",
    "psycopg[binary]>=3.1",
    "celery[redis]>=5.4",
    "django-celery-beat>=2.6",
    "django-mail-auth>=0.3",
    "django-health-check>=3.18",
    "django-environ>=0.11",
]
