{$DOMAIN} {
    encode gzip

    # Proxy everything to the Django app
    reverse_proxy web:8000

    # Serve media files directly if you ever bind-mount them into Caddy
    # handle_path /media/* {
    #     root * /srv/media
    #     file_server
    # }

    log {
        output stdout
        format json
    }
}
