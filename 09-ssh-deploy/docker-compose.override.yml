services:
  web:
    build:
      target: dev
    volumes:
      - .:/app
      - /app/.venv

  worker:
    build:
      target: dev
    command: python manage.py rqworker default
    volumes:
      - .:/app
      - /app/.venv

  db:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"
