services:
  web:
    build:
      target: dev
    volumes:
      - .:/app

  worker:
    build:
      target: dev
    volumes:
      - .:/app

  db:
    # Expose port for host GUI access when needed
    # ports:
    #   - "127.0.0.1:5433:5432"
