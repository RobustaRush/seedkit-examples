dbs:
  - path: ${DB_PATH:-/data/db.sqlite3}
    replicas:
      - type: s3
        bucket: ${LITESTREAM_S3_BUCKET}
        path: db.sqlite3
        endpoint: ${LITESTREAM_S3_ENDPOINT:-}
        access-key-id: ${LITESTREAM_ACCESS_KEY_ID}
        secret-access-key: ${LITESTREAM_SECRET_ACCESS_KEY}
